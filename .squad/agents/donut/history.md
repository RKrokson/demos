# Project Context

- **Owner:** Ryan Krokson
- **Project:** Azure IaC demo/lab environments — Terraform modules for Azure vWAN, AI Foundry, networking
- **Stack:** Terraform (azurerm >= 4.0, azapi >= 2.0, random ~> 3.5), PowerShell, Azure CLI
- **Structure:** Three root modules — Networking (foundation), Foundry-byoVnet, Foundry-managedVnet linked via terraform_remote_state
- **Created:** 2026-03-27

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
- **2026-03-27 (gitignore-audit):** Appended preventive .gitignore patterns for IDE configs (`.vscode/`, `.idea/`, swap files), environment files (`.env`, `.env.local`, `.env.*.local`), and OS artifacts (`Thumbs.db`, `.DS_Store`). Existing Terraform and Squad patterns left untouched. Coordinated with Carl (lead audit) and Katia (security validation).
- **2026-03-27 (terraform-best-practices-audit):** Comprehensive review of all 12 .tf files across 3 root modules. Key patterns: Networking/main.tf is 1020 lines (monolith), ~120 variables in Networking with heavy region duplication (region 0/1 copy-paste). Foundry modules use azapi extensively for AI Foundry resources not yet in azurerm. No tags on any resources. No `required_version` in Networking/config.tf. Local state backend used everywhere. Firewall rules are allow-all wildcards. Private DNS zone IDs are constructed via string interpolation rather than data sources. Variables lack validation blocks. `vm_admin_username` output exposes potentially sensitive info. `random_password` not marked sensitive. `s2s_site00_speed` typed as string but should be number.
- **2026-03-29 (repo-revamp-plan):** Full team review finalized. Four high-priority bugs identified (VPN naming, three count-guard crashes). Comprehensive recommendations delivered across code quality, security hardening, and documentation. Team consensus: implement critical bugs first, then architectural changes. All findings merged into squad decisions.md. **Key output:** 40+ prioritized findings with implementation sequence. **Cross-team coordination:** Worked with Carl (lead), Katia (validator), Mordecai (docs) on integrated review.
- **2026-03-29 (phase1-implementation):** Executed Phase 1 (5 tasks). **Task 1 (1-bugs):** Removed all VPN resources (8 resource blocks from main.tf), 16 VPN variables from variables.tf, VPN toggles from all tfvars files. Fixed fw01_logs count guard (added `create_vhub01 &&`). Fixed ai_vnet01_dns count guard (added `var.create_vhub01 &&`). **Task 2 (1-tags):** Added `default_tags` block to all 3 provider configs (Networking, Foundry-byoVnet, Foundry-managedVnet). **Task 3 (1-outputs):** Expanded Networking/outputs.tf with 10 DNS zone IDs, vHub IDs, LAW ID, Key Vault ID/name, Region 1 subnet IDs. Replaced all string-interpolated DNS zone IDs in both Foundry modules with direct output references. Created outputs.tf in both Foundry modules. **Task 4 (1-provider):** Added `required_version >= 1.8.3` to Networking/config.tf. Added `random` provider declaration to Foundry-byoVnet/config.tf. Added explanatory comments to `prevent_deletion_if_contains_resources = false` in all 3 configs. **Task 5 (1-fmt):** Ran `terraform fmt` across all modules. Key pattern: DNS zone IDs are now part of the Networking output contract, eliminating brittle ARM path construction in downstream modules.
- **2026-03-30 (phase1-coordination):** Coordinated Phase 1 implementation with Mordecai (docs). Established VPN removal output contract in decisions.md. Merged decision inbox (copilot-directive on humanizer rules, vpn-removal-output-contract) into main decisions file. Updated Mordecai history with cross-team VPN coordination note. All changes ready for git commit with message "squad: phase 1 implementation — VPN removal, bug fixes, tags, outputs, LZ framing".
- **2026-03-30 (tagging-pattern-locked):** Carl completed tagging implementation lockout (59 resources tagged with local.common_tags). Established durable pattern: azurerm tagging uses locals + explicit per-resource assignment, NEVER default_tags block (AWS provider only). Foundry output naming standardization approved (rename Foundry-managedVnet to use ai_foundry_ prefix). Decision inbox merged to main decisions file, deduplicating tagging-strategy and foundry-output-naming entries.
