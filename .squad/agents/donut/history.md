# Project Context

- **Owner:** Ryan Krokson
- **Project:** Azure IaC demo/lab environments — Terraform modules for Azure vWAN, AI Foundry, networking
- **Stack:** Terraform (azurerm >= 4.0, azapi >= 2.0, random ~> 3.5), PowerShell, Azure CLI
- **Structure:** Three root modules — Networking (foundation), Foundry-byoVnet, Foundry-managedVnet linked via terraform_remote_state
- **Created:** 2026-03-27

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->
- **2026-03-27 (gitignore-audit):** Appended preventive .gitignore patterns for IDE configs (`.vscode/`, `.idea/`, swap files), environment files (`.env`, `.env.local`, `.env.*.local`), and OS artifacts (`Thumbs.db`, `.DS_Store`). Existing Terraform and Squad patterns left untouched. Coordinated with Carl (lead audit) and Katia (security validation).
- **2026-03-27 (terraform-best-practices-audit):** Comprehensive review of all 12 .tf files across 3 root modules. Key patterns: Networking/main.tf is 1020 lines (monolith), ~120 variables in Networking with heavy region duplication (region 0/1 copy-paste). Foundry modules use azapi extensively for AI Foundry resources not yet in azurerm. No tags on any resources. No `required_version` in Networking/config.tf. Local state backend used everywhere. Firewall rules are allow-all wildcards. Private DNS zone IDs are constructed via string interpolation rather than data sources. Variables lack validation blocks. `vm_admin_username` output exposes potentially sensitive info. `random_password` not marked sensitive. `s2s_site00_speed` typed as string but should be number.
- **2026-03-29 (repo-revamp-plan):** Full team review finalized. Four high-priority bugs identified (VPN naming, three count-guard crashes). Comprehensive recommendations delivered across code quality, security hardening, and documentation. Team consensus: implement critical bugs first, then architectural changes. All findings merged into squad decisions.md. **Key output:** 40+ prioritized findings with implementation sequence. **Cross-team coordination:** Worked with Carl (lead), Katia (validator), Mordecai (docs) on integrated review.
- **2026-03-29 (phase1-implementation):** Executed Phase 1 (5 tasks). **Task 1 (1-bugs):** Removed all VPN resources (8 resource blocks from main.tf), 16 VPN variables from variables.tf, VPN toggles from all tfvars files. Fixed fw01_logs count guard (added `create_vhub01 &&`). Fixed ai_vnet01_dns count guard (added `var.create_vhub01 &&`). **Task 2 (1-tags):** Added `default_tags` block to all 3 provider configs (Networking, Foundry-byoVnet, Foundry-managedVnet). **Task 3 (1-outputs):** Expanded Networking/outputs.tf with 10 DNS zone IDs, vHub IDs, LAW ID, Key Vault ID/name, Region 1 subnet IDs. Replaced all string-interpolated DNS zone IDs in both Foundry modules with direct output references. Created outputs.tf in both Foundry modules. **Task 4 (1-provider):** Added `required_version >= 1.8.3` to Networking/config.tf. Added `random` provider declaration to Foundry-byoVnet/config.tf. Added explanatory comments to `prevent_deletion_if_contains_resources = false` in all 3 configs. **Task 5 (1-fmt):** Ran `terraform fmt` across all modules. Key pattern: DNS zone IDs are now part of the Networking output contract, eliminating brittle ARM path construction in downstream modules.
- **2026-03-30 (phase1-coordination):** Coordinated Phase 1 implementation with Mordecai (docs). Established VPN removal output contract in decisions.md. Merged decision inbox (copilot-directive on humanizer rules, vpn-removal-output-contract) into main decisions file. Updated Mordecai history with cross-team VPN coordination note. All changes ready for git commit with message "squad: phase 1 implementation — VPN removal, bug fixes, tags, outputs, LZ framing".
- **2026-03-30 (tagging-pattern-locked):** Carl completed tagging implementation lockout (59 resources tagged with local.common_tags). Established durable pattern: azurerm tagging uses locals + explicit per-resource assignment, NEVER default_tags block (AWS provider only). Foundry output naming standardization approved (rename Foundry-managedVnet to use ai_foundry_ prefix). Decision inbox merged to main decisions file, deduplicating tagging-strategy and foundry-output-naming entries.
- **2026-03-30 (phase2-implementation):** Executed Phase 2 across three task groups. **Task 1 (file-split):** Split Networking/main.tf (953 lines) into 7 domain files: vwan.tf, firewall.tf, dns.tf, compute.tf, ai-spoke.tf, keyvault.tf, plus trimmed main.tf. Split Foundry-byoVnet/main.tf into storage.tf, cosmosdb.tf, aisearch.tf, foundry.tf, project.tf, rbac.tf. Split Foundry-managedVnet/main.tf into 7 files adding networking.tf. Zero resource address changes. **Task 2 (conditionals):** Merged 6 secure/unsecure vHub connection pairs into 3 single resources using `internet_security_enabled = var.add_firewallXX`. Removed inter-PE depends_on chains in byoVnet. Added 60m timeouts to firewalls and vHubs. Added PG-validated comments to all 7 time_sleep resources. **Task 3 (security):** Marked vm_admin_username output as sensitive. Added allow-all WARNING comments to both firewall RCGs. Set disableLocalAuth=true on AI Search in both Foundry modules. Changed networkAcls.defaultAction from Allow to Deny on byoVnet foundry resource. All modules pass `terraform fmt` and `terraform validate`. **Key learning:** `random_password.result` is already sensitive in random provider ~>3.5 — no explicit marking needed on the resource. vHub connection merging eliminated 6 resource blocks and simplified the conditional logic significantly.
- **2026-03-30 (phase2-completion):** Phase 2 batch 1 complete. Orchestration log written. Decision inbox merged to decisions.md with four new entries (local-auth-research, vhub-connection-merge, github-org-standardization, foundry-alz-directive). Ready for git commit with message "squad: phase 2 batch 1 — file splits, conditionals, security, docs".
- **2026-03-30 (phase2-batch2):** Executed 3 tasks across all modules. **Task 1 (2-locals):** Created Networking/locals.tf with `suffix`, `rg00_name`, `rg00_location` extracting 3 repeated patterns. Replaced ~40 inline references across 7 domain files. Region 1 equivalents skipped — conditional indexing (`[0]`) means locals wouldn't reduce verbosity. **Task 2 (2-variables):** Renamed 8 PascalCase/mixed-case variables to snake_case (`firewall_SkuName00/01`, `firewall_SkuTier00/01`, `resource_group_name_KV`, `create_AiLZ`, `add_privateDNS00/01`). Added validation blocks to 6 variables (2 CIDR, 2 bastion SKU, 2 firewall SKU tier). Fixed vm00_nic_name description typo. Made 18 generic descriptions unique (VNet names, firewall names, DNS resolver names). Updated all 3 tfvars files. **Task 3 (2-consistency):** Renamed `random_string.myrandom` to `random_string.unique` in Networking (matching Foundry convention). Renamed `ai_foundry` resource label to `foundry` in Foundry-byoVnet (matching managedVnet convention). Added provider version comment explaining Networking's wider `>= 4.0, < 5.0` range. Changed byoVnet storage from ZRS to LRS per Ryan's confirmation. Added `["AzureServices"]` bypass to managedVnet storage. Standardized managedVnet output names to `ai_foundry_` prefix. All 3 modules pass `terraform fmt` and `terraform validate`. **Coordination:** Scribe logged Phase 2 Batch 2 completion, merged decision inbox entries (resource-label-standardization, variable-naming-conventions, storage-account-configuration-pattern), updated agent history, prepared git commit. **Outcome:** SUCCESS — all code changes validated, 4 new decisions documented, ready for merge.
- **2026-03-30 (phase3-depends-cleanup):** Removed 19 redundant `depends_on` blocks across all 3 modules where implicit dependencies already existed via attribute references (e.g., `cognitive_account_id = azapi_resource.foundry.id` already creates the dependency). Kept all `depends_on` on `time_sleep` resources (side-effect ordering), RBAC propagation delays, capability host prerequisites, and true ordering-only dependencies where no attribute reference exists. Key pattern: Private endpoints always reference their parent resource's `.name` and `.id`, making `depends_on` redundant. Child resources using `parent_id` also have implicit deps. Connection resources using `parent_id = azapi_resource.foundry_project.id` don't need explicit `depends_on`. Phase 3 wrap: Scribe logged orchestration entry, session log, updated now.md, appended this learning to history. Full repo revamp complete (3 phases, 22 work items).
- **2026-03-30 (phase3-hardcoded-extraction):** Extracted 4 hardcoded values from Networking (dns_forwarder_ip, firewall_availability_zones, log_analytics_sku, log_analytics_retention_days) and 7 per Foundry module (gpt_model_deployment_name, gpt_model_name, gpt_model_version, gpt_model_sku_name, gpt_model_capacity, ai_search_sku, foundry_sku). All variables have current hardcoded values as defaults — zero behavioral change. All 3 modules pass `terraform fmt` and `terraform validate`. Scribe coordinating Phase 3 wrap-up orchestration (log entries, agent history updates, now.md update, git commit). Ready for merge.
- **2026-03-30 (firewall-toggle-addition):** Added internet_security_enabled toggle to AI spoke vHub connections in both Foundry modules (byoVnet, managedVnet). When firewall00 is enabled, AI spoke traffic routes through it; when disabled, traffic bypasses security inspection. Aligns with Phase 2 security hardening pattern and enables explicit control over Foundry workload firewall participation. Scribe logged orchestration entry and merged Decision #8 into decisions.md.
- **2026-07-26 (alz-vnet-refactor):** Implemented Carl's ALZ VNet design. Moved AI spoke VNet, 2 subnets, vHub connection, DNS server config, and DNS policy link from Networking/ai-spoke.tf into new networking.tf files in both Foundry modules. Removed `create_ai_lz` variable and all AI LZ variables (region 0 and 1) from Networking. Deleted ai-spoke.tf entirely. Removed 2 DNS resolver policy AI VNet links from dns.tf. Removed 4 subnet outputs from outputs.tf. Added 4 new Networking outputs: `rg_net00_name`, `add_firewall00`, `dns_resolver_policy00_id`, `dns_inbound_endpoint00_ip`. Each Foundry module gets 8 new variables (VNet name, address space, 2 subnet names/addresses, `connect_to_vhub`, `enable_dns_link`) with distinct default CIDRs (BYO=Block 2 172.20.32.0/20, Managed=Block 3 172.20.48.0/20). Added NSG on PE subnet in both modules. Updated all PE/foundry subnet references from remote_state to local resources. Updated all 3 tfvars files. Created docs/ip-addressing.md. All 3 modules pass `terraform fmt` and `terraform validate`. **Key learning:** VNet ownership belongs in the app LZ, not the platform module. `internet_security_enabled` reads from Networking output (`add_firewall00`) so firewall decision stays in the platform layer.
- **2026-07-26 (depends-on-cleanup):** Removed 19 redundant `depends_on` blocks across all 3 modules where implicit dependencies already existed via attribute references (e.g., `cognitive_account_id = azapi_resource.foundry.id` already creates the dependency). Kept all `depends_on` on `time_sleep` resources (side-effect ordering), RBAC propagation delays, capability host prerequisites, and true ordering-only dependencies where no attribute reference exists. **Key pattern:** Private endpoints always reference their parent resource's `.name` and `.id`, making `depends_on` redundant. Child resources using `parent_id` also have implicit deps. Connection resources using `parent_id = azapi_resource.foundry_project.id` don't need explicit `depends_on`.
- **2026-07-26 (hardcoded-extraction):** Extracted 4 hardcoded values from Networking (dns_forwarder_ip, firewall_availability_zones, log_analytics_sku, log_analytics_retention_days) and 7 per Foundry module (gpt_model_deployment_name, gpt_model_name, gpt_model_version, gpt_model_sku_name, gpt_model_capacity, ai_search_sku, foundry_sku). All variables have current hardcoded values as defaults — zero behavioral change. All 3 modules pass `terraform fmt` and `terraform validate`.
- **2026-07-27 (region-hub-module):** Implemented Carl's `modules/region-hub/` child module design. Created 3 files (main.tf, variables.tf, outputs.tf) encapsulating 31 per-region resources (hub, shared VNet/subnets, firewall, DNS resolver, compute). Replaced duplicated region 0/1 resource blocks in root with two module calls (`module.region0` always-on, `module.region1` gated by `create_vhub01`). Deleted 3 now-empty root files (firewall.tf, dns.tf, compute.tf). Trimmed vwan.tf to vWAN-only. Updated 5 outputs to reference module outputs. Added `firewall_availability_zones` and `dns_forwarder_ip` as child module variables (not in Carl's design but required by resources). All 3 modules pass `terraform fmt` and `terraform validate`. **Key pattern:** Module boundary structurally eliminates Katia's count-guard bugs — region 1's `count` on the module means internal resources only need their own feature toggle (`add_firewall`, `add_private_dns`), not nested `create_vhub01 ? (...) : 0`.
- **2026-03-30 (region-module-spawn-wrap):** Scribe wrote orchestration log entry (2026-03-30T20-30-region-module-spawn.md) documenting region child module implementation outcome (SUCCESS). Merged decision inbox entries (region-module-directive, region-module-vars-extension, depends-on-cleanup, hardcoded-extraction) into decisions.md as Decision #9 (Region Hub Child Module), Decision #10 (User Directive), Decision #11 (depends_on Rules), and Decision #12 (Region Module Variables Extension). Updated Katia and Mordecai history with cross-team module coordination notes. Prepared git commit: "squad: region child module implemented" with commit message from temp file.
- **2026-07-27 (dns-proxy-firewall):** Implemented DNS proxy on Azure Firewall feature. Added `dns { proxy_enabled = true }` block to firewall policy in child module with conditional `servers` (points to resolver inbound endpoint when private DNS is deployed). Changed shared VNet DNS to compute internally: when firewall is deployed, VNet DNS points to firewall private IP; otherwise, to resolver inbound endpoint. Removed `shared_vnet_dns_servers` variable from child module (and `shared_vnet00_dns`/`shared_vnet01_dns` from root) since DNS is now computed. Added `firewall_private_ip00` output to Networking root. Updated both Foundry modules' networking.tf to conditionally use firewall IP or resolver IP for VNet DNS. DNS VNet intentionally NOT pointed at firewall (would create circular dependency since resolver lives there). Cleaned up tfvars examples. All 3 modules pass `terraform fmt` and `terraform validate`. **Key pattern:** vHub-based firewall private IP is at `virtual_hub[0].private_ip_address`, not `ip_configuration[0].private_ip_address` (that's VNet-mode firewalls).
- **2026-07-27 (foundry-dns-simplification):** Removed unnecessary conditionals from both Foundry modules per Ryan's review. Deleted `connect_to_vhub` and `enable_dns_link` variables — vHub connection, DNS server config, and DNS resolver policy link are now unconditional (always created). Added `dns_server_ip00` output to Networking that consolidates the firewall-vs-resolver DNS IP decision in the platform layer. Foundry modules now consume a single IP instead of containing their own conditional logic. Kept `add_firewall00` output — still needed for `internet_security_enabled` on vHub connections. Removed references from both tfvars example files. All 3 modules pass `terraform fmt` and `terraform validate`. **Key pattern:** DNS routing decisions belong in the platform landing zone, not the app landing zone. App LZs should consume a single "use this DNS server" output.
- **2026-07-29 (full-environment-teardown):** Executed full teardown sequence for suffix 8377 (Networking) and 9741 (Foundry-byoVnet). **Step 1 — Foundry-byoVnet destroy:** 28/29 resources destroyed (~15 min). CosmosDB took 12 min alone. One failure: `ai-foundry-subnet-sece` blocked by `legionservicelink` service association (known Azure issue — link takes time to clear after AI Foundry deletion). **Step 2 — AI Foundry purge:** `az cognitiveservices account purge` succeeded for `aifoundry9741` in `swedencentral`. Took ~2 min. **Step 3 — 15-min wait:** 21:49–22:04 for purge propagation. **Step 4 — Networking destroy:** 563 resources destroyed (~45 min). vHub took 10 min, vWAN 20 sec. One failure: `myDnsSecurityPolicy-sece` DNS resolver policy blocked by orphan VNet links from RGs `rg-ai00-sece-0007` and `rg-ai00-sece-1253` (stale from previous Foundry deployments). **Step 5 — Orphan RG cleanup:** Fired `az group delete --no-wait` for both orphan RGs (0007, 1253). Commands accepted without error. **Key learnings:** (1) CosmosDB is the slowest Foundry resource to delete (~12 min). (2) AI Foundry legionservicelink on subnets can persist after destroy — may need manual retry or time. (3) DNS resolver policies can't be deleted while orphan VNet links exist from previously destroyed Foundry LZs. (4) Total teardown wall time: ~75 min excluding the 15-min purge wait. (5) Destroy order matters: Foundry LZ first, purge, wait, then Networking.
- **2026-07-30 (networking-destroy-retry):** Retried `terraform destroy` for Networking LZ after Ryan manually removed orphan DNS resolver policy VNet links (from RGs 0007, 1253) that blocked last night's destroy. **Result: SUCCESS.** 3 remaining resources destroyed: `myDnsSecurityPolicy-sece` (9s), `rg-net00-sece-8377` (66s), `random_string.unique` (instant). Confirmed `rg-ai00-sece-9741` (Foundry RG) is also gone — `ResourceGroupNotFound` from Azure, meaning the legionservicelink cleared and the RG was fully deleted. **Full environment is now clean.** **Key learning:** After orphan VNet link removal, DNS resolver policy deletes quickly (~9s). The legionservicelink issue on the Foundry subnet also resolved itself overnight without manual intervention.
- **2026-07-30 (primary-region-deploy):** Deployed full environment — primary region only (Sweden Central) with Firewall + DNS enabled. **Networking LZ:** 578 resources created, suffix `1150`, ~35 min wall time. vWAN hub, Azure Firewall (IP 172.30.0.132), DNS resolver (inbound 172.20.16.4), shared VNet, bastion, compute VM, Key Vault, Log Analytics, 10 private DNS zones. **Foundry-byoVnet:** 31 resources created, suffix `9741` (reused from previous state), ~25 min wall time. AI Foundry (`aifoundry9741`), project, CosmosDB, AI Search, storage account, spoke VNet (172.20.32.0/20), 6 private endpoints, vHub connection with firewall routing, DNS resolver policy link. GPT model: gpt-5.4 (GlobalStandard, capacity 1). No errors on either deployment. Secondary region (centralus) intentionally disabled via `create_vhub01 = false`. **Total deploy time:** ~60 min.
- **2026-07-30 (full-environment-teardown-2):** Tore down entire environment (suffix `9741` Foundry, suffix `1150` Networking). **Step 1 — Foundry-byoVnet destroy:** First run hit transient 409 Conflict on AI Search connection delete (ETag race). Retry destroyed most resources but `ai-foundry-subnet-sece` blocked by `legionservicelink` (same known issue). **Step 2 — AI Foundry purge:** Purged 3 soft-deleted Cognitive Services accounts (`aifoundry9741`, `aifoundry0007`, `aifoundry1253`) via `az cognitiveservices account purge`. **Step 3 — Subnet link persistence:** Even after purge + 5 min wait, the `legionservicelink` didn't release. Workaround: deleted RG `rg-ai00-sece-9741` directly via `az group delete`, then cleaned Terraform state with `terraform state rm`. **Step 4 — Networking destroy:** 578 resources destroyed cleanly in ~30 min. vHub took ~10 min, vWAN 11s. No errors. **Key learnings:** (1) The `legionservicelink` can persist stubbornly even after purge — fallback is to delete the RG via CLI and clean state. (2) Transient 409 errors on Foundry connection deletes are recoverable on retry. (3) Always purge ALL stale soft-deleted accounts (not just the current one) before proceeding. (4) Total teardown wall time: ~50 min including waits.
- **2026-07-30 (full-environment-deploy-3):** Deployed full environment — primary region only (Sweden Central) with Firewall + DNS enabled. **Networking LZ:** 578 resources created, suffix `0470`, ~35 min wall time. vWAN hub (`vhub00-sece`), Azure Firewall (private IP `172.30.0.132`), DNS resolver (inbound `172.20.16.4`), shared VNet, bastion, compute VM, Key Vault (`kv00-sece-0470`), Log Analytics (`law00-sece`), 10 private DNS zones. RG: `rg-net00-sece-0470`. **Foundry-byoVnet:** 32 resources created, suffix `2585`, ~20 min wall time. AI Foundry (`aifoundry2585`), project (`project2585`), CosmosDB (`aifoundry2585cosmosdb`), AI Search (`aifoundry2585search`), storage (`aifoundry2585storage00`), spoke VNet (`ai-vnet-sece-2585`, 172.20.32.0/20), 6 private endpoints, vHub connection with firewall routing, DNS resolver policy link. GPT model: gpt-5.4 (GlobalStandard, capacity 1). RG: `rg-ai00-sece-2585`. No errors on either deployment. **Total deploy time:** ~55 min.
